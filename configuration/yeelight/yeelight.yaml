devices:
  192.168.1.121:
    name: Bedroom Main Light
    transition: 10
    use_music_mode: true #(defaults to False)
    save_on_change: true #(defaults to True)
  192.168.1.120:
    name: Bedroom Main Light 2
    transition: 10
    use_music_mode: true #(defaults to False)
    save_on_change: true #(defaults to True)
  custom_effects:
    - name: 'Fire Flicker'
      flow_params:
        count: 0
        transitions:
          - TemperatureTransition: [1900, 1000, 80]
          - TemperatureTransition: [1900, 2000, 60]
          - SleepTransition:       [1000]